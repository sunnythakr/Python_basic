# # Remove multiple elements from a list in Python
# lst = [int(l) for l in range("List: ").split(",")]
lst = [2,3,4,5,6,7]
# b = {4,5} removing multiple item using dict 
b = 6  # remove single item simple assign value
lst.remove(b)
print(lst)
